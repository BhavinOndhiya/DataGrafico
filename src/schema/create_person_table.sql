-- SQL script for creating the person table
CREATE TABLE IF NOT EXISTS person (
    din TEXT,
    name TEXT,
    begin_date DATE,
    end_date DATE,
    cin TEXT,
    person_date TEXT,
    day INTEGER,
    month INTEGER,
    year INTEGER
);
